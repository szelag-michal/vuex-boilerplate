const base = 'https://lakomy-lasuch.firebaseio.com/'

const url = {
  ingredients: `${base}/ingredients.json`,
  categories: `${base}/categories.json`,
  posts: `${base}/posts.json`
}

export default url